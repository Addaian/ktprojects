# Purpose 
- Add the headers into mobile version to allow users to navigate between pages 

# Implementation 
- Make this a drop down menu in the same level as the current header with the logo
- The drop down will have all the pages in the same font and style as the web design

# Fixes 
- Right now the header is wrapped on top of the logo so as the width is shrink some times these headers overlap with the logo 
- Ensure that once the width shrinks to the point where the header is very close to the logo it becomes the drop down instead